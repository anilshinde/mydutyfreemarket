        <!-- Top menu -->
        <nav class="navbar" role="navigation">
            <div class="container">
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#top-navbar-1" aria-expanded="false">
                        <span class="sr-only">Navigation</span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                    <a class="navbar-brand" href="{{ site.url }}">{{ site.name }}</a>
                </div>

                <!-- Collect the nav links, forms, and other content for toggling -->
                <div class="collapse navbar-collapse" id="top-navbar-1">
                    <ul class="nav navbar-nav navbar-right" itemscope itemtype="http://schema.org/BreadcrumbList">
                        {% for category in categoriesTree %}
                        <li class="dropdown {% if category.name == currentCategory.name %} active" itemprop="itemListElement" itemscope itemtype="http://schema.org/ListItem" {% else %} " {% endif %}>
                            <a {% if category.name == currentCategory.name %} itemprop="item" {% endif %} href="{{ category.url }}">
                                <span {% if category.name == currentCategory.name %} itemprop="name" {% endif %}>{{ category.name }}</span>
                            </a>
                        </li>
                            {% if category.subcategories is not empty %}
                            <meta itemprop="position" content="1"> 
                            <ul class="dropdown-menu dropdown-menu-left" role="menu">
                                {% for subcategory in category.subcategories %}
                                <li><a href="{{ subcategory.page.url }}">{{ subcategory.name }}</a></li>
                                {% endfor %}
                            </ul>
                            {% endif %}
                        </li>
                        {% endfor %}
                    </ul>
                </div>
            </div>
        </nav>
